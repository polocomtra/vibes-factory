"use client";

import MCPServersPage from "../page";

/**
 * Detail URLs are kept stable for deep links; the responsive detail panel is
 * rendered by the shared MCP server surface.
 */
export default function MCPServerDetailPage() {
  return <MCPServersPage />;
}
